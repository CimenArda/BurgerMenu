@model List<ProjectBurgerMenu.Entities.Product>
@{
    int count = 0;
}
@Html.Action("PartialHead", "Default")

<div class="tab-content text-left" style="margin-top: 50px;">
    <!-- Sayfanın biraz daha altına almak için marjin eklendi -->
    <div class="tab-pane fade show active" id="pills-breakfast" role="tabpanel" aria-labelledby="pills-breakfast-tab">
        <div class="row justify-content-center">
            <!-- Ürünlerin ortalanması için justify-content-center eklendi -->

            @foreach (var item in Model)
            {
                <div class="col-md-6 site-animate">
                    <div class="media menu-item">
                        <img class="mr-3" src="@item.ImageUrl" class="img-fluid" alt="Free Template by colorlib.com">
                        <div class="media-body">
                            <h5 class="mt-0">@item.ProductName</h5>
                            <p>@item.Description</p>
                            <h6 class="text-primary menu-price">@item.Price TL</h6>
                        </div>
                    </div>
                </div>
            }
        </div>
    </div>
</div>

@Html.Action("PartialScript", "Default")
